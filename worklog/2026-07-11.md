# 工作日志 · 2026-07-11

接 2026-07-10 的收尾 + VM 重启恢复。工作量不大，主要是运维恢复和仓库整理。

## 今天做完的

### VM 重启恢复
- Lima 虚机 `my-frr` 被发现处于 Stopped，`limactl start` 拉起。
- 重启后 **4 台 cEOS 路由器(core1/2、edge1/2)数据口 Et1/2/3 断了**（只剩 Management0）——cEOS 在宿主机重启后 containerlab 的 veth 链路不自动恢复，是已知坑。交换机/pc/inet 都正常回来。`docker start` 能让容器 Up，但数据链路仍断，OSPF/BGP/连通性未恢复。

### 固化 BGP（完成 TODO a）
- 趁路由器还能通（Management0），把 4 台的 iBGP/eBGP + Loopback 配置从 running-config 抢救出来，写进 `configs/core1/2.cfg`、`configs/edge1/2.cfg`（VM + 仓库）。
- 意义：以后 `containerlab destroy/deploy` 重建时 BGP 会自动回来，不再需要手动重配。

### 仓库整理
- 应要求把 **Claude 从 GitHub contributors 去掉**：改写全部 11 个提交历史，删除 `Co-Authored-By: Claude ...` 尾注（正文保留、author 仍是本人），force-push。contributors 现只剩 `kedicat308`。
- 注意：**后续对本仓库的提交不要再加 Claude 尾注**，否则会把 Claude 加回 contributors。

## 当前实验状态（离开时）
- VM：✅ 已启动。
- 交换机 / pc / inet：✅ 运行。
- **4 台 cEOS 路由器：⚠️ 容器 Up 但数据链路断**，OSPF/BGP/连通性未恢复。
- 遥测栈：✅ 运行（gNMI 走 Management0 可达，但路由器暂无数据面流量）。
- BGP 配置：✅ 已进 .cfg，重建不丢。

## 下次开工先做（恢复实验）
```bash
limactl start my-frr                 # 若 VM 又停了
sudo containerlab deploy -t campus.yml   # 重新连线 + 从 .cfg 重建（BGP 已在里面）
bash setup-services.sh               # pc 服务 / NAT / socat / 遥测栈
```

## 仍未完成的 TODO
- **inet 的 FRR（eBGP ISP 侧）还没进 setup-services.sh** → 重建后 eBGP 仍需手动重配。下次优先补这个，才能真正一键全恢复。
- **(c)** OSPF 监控：gNMI 拿不到，需 eAPI 小 exporter。
- 可选：Loki 日志采集（指标+日志+操作命令三线合一）。

## 下班清点
- ✅ 代码/配置/文档已推 GitHub，contributors 已清理。
- ⚠️ 实验处于"半恢复"状态，下次按上面三条命令拉起即可（BGP 自动回来，FRR 需手动补）。
