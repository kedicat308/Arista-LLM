# 工作日志 · 2026-07-10

园区网实验 + 可观测性栈。今天从"只有指标"推进到"路由 + BGP 监控 + 排障闭环 + 发布素材"，一路踩坑一路记。

## 今天做完的

### 路由
- **iBGP**（core1/core2，AS 65001）：基于 Loopback 对等，OSPF 提供可达性，叠加不抢转发（iBGP AD 200 > OSPF 110）。
- **eBGP**（edges ↔ inet，AS 65001 ↔ 65100）：把 inet 用 **FRR** 变成 ISP 路由器，宣告 `0.0.0.0/0` + 假互联网前缀（203.0.113/198.51.100，RFC5737）；edges 宣告园区网段。踩坑：FRR 8.x 的 **RFC 8212** 默认拒绝（`no bgp ebgp-requires-policy`）；EOS **eBGP AD=200**（非 Cisco 的 20）。

### 可观测性
- **BGP 监控上线**：gNMI 订阅 session-state（on-change）+ prefix 计数（sample）。session-state 是 6 态 FSM，映射成 `1..6`（IDLE..ESTABLISHED），**不压成 1/0**——现场演示证明价值：关邻居时 core1=Idle(1) / core2=Active(3) 两端不同态，能分清"谁关的 vs 谁连不上"。Grafana 加了 2 个面板（状态时间线 + 前缀数）。
- **修基数泄漏**：每进程遥测 × rsyslog pid churn × 6h 过期 → 序列累积到 ~4000 还在涨。修法：`expiration 6h→60s`（截断累积，活跃序列封顶 ~1435 稳定）+ 看板 `proc=~"[A-Z].*"` 只显真 EOS agent。proc_info 3949→364。

### 排障（三个案例，方法论闭环）
- **幻影内存尖峰（13:52）**：日志全空 → 时间序列定位 ConfigAgent+Sysdb 同步 +54MB → 实验证伪 CLI 读、证实 **gNMI 批量读**（读操作不留日志，只有写才记）。→ 已写成 `diag_case/diagnosis.md`。
- **rsyslog 崩溃循环 248 万次**：systemd socket-activation 与 rsyslog 抢 `/run/systemd/journal/syslog`，EADDRINUSE + 限流关闭 → 每 100ms 重启不止。无害但制造 pid churn（正是上面基数泄漏的源头）。
- **18:35 内存/CPU 抬升**：定位为 BGP agent（Bgp-main，四台合计 ~1.5G）启动的正常开销 + 配置验证活动，非泄漏。

### 文档 / 仓库（github.com/kedicat308/Arista-LLM）
- `GUIDE.md` — 完整构建运维指导书（15 章）。
- `diag_case/diagnosis.md`（中英双语）+ `diag_case/devto_article.md`（可发布版）。
- 今日多次同步 telemetry 配置（gnmic.yaml / gnmic-proc.yaml / campus.json）。

### 发布素材
- LinkedIn 长文 + 引言、X thread、Reddit 版、dev.to 正式版（幻影尖峰那篇）。Reddit 首发被过滤器拦（新号 + 外链）——建议先 dev.to 立 canonical 再引流。

## 当前实验状态（离开时）
- 15 个容器运行中，OSPF + iBGP + eBGP 全 Established，pc1→pc3、pc1→出网都通。
- 遥测栈跑着：Grafana `localhost:3000`（11 面板）、Prometheus `localhost:9090`。
- 每进程遥测已修，序列稳定 ~1435。

## 持久化状态（重要）
| 内容 | 重启 | 重部署 | 说明 |
|---|---|---|---|
| OSPF/gNMI/用户 | ✅ | ✅ | 在 configs/*.cfg |
| **iBGP/eBGP** | ✅（write mem） | ❌ | 未写进 configs/*.cfg |
| **FRR (ISP)** | ✅（frr.conf） | ❌ | 未进 setup-services.sh |
| 遥测栈 | ✅ | ✅ | docker compose |
| pc服务/NAT/socat | ❌ | ❌ | setup-services.sh |

## 下次待办（TODO）
- **(a)** 固化 BGP 进 `configs/core*.cfg`/`edge*.cfg`；FRR 装配进 `setup-services.sh` → 真正一键全恢复。
- **(c)** OSPF 监控：gNMI 拿不到，需写个 eAPI 小 exporter（`show ip ospf neighbor` → Prometheus）。
- 可选：日志采集（Loki + promtail + 操作命令 annotation），把"指标+日志+变更流"三线合到一个 Grafana 时间轴——之前讨论过，能让排障从"猜+复现"变"一眼定位"。

## 下班清点
- ✅ 所有代码/配置/文档已推 GitHub（最新 commit `1b349bd`）。
- ✅ 实验保持运行（无需清理；重启能扛，重部署需按上面 TODO 恢复）。
